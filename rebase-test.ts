class RebaseTest{
    x=10;
    y=20;
    z=30;
    a=10;
    b=20;
    printValues() {
        console.log(`x = ${this.x}, y = ${this.}, z = ${this.z}, a=${this.a}, b=${this.b}`);
    }
}

// Create an instance and call the method
const test = new RebaseTest();
test.printValues();

